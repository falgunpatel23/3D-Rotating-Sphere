# 3D-Rotating-Sphere
This Python project creates a 3D sphere by generating vertex coordinates using spherical math functions. The sphere is rendered using custom code in Pygame and rotates over time.
